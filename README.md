# ClockIn (MVP)

Menu bar time tracker for macOS built with SwiftUI.

## Run
Open this folder in Xcode and run the `ClockIn` executable target on macOS 13+.

## Notes
- Data is stored locally using `UserDefaults`.
- Segments are split at midnight for daily totals.
